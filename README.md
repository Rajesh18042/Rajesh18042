# Registrtion-Form
```sql
create table project (fname varchar(30) not null,
lname varchar(30) not null,
password varchar(30) not null,
address varchar(30),
unumber varchar(30),
email varchar(30) not null,
mobile varchar(30) not null,
dob varchar(30) not null,
gender varchar(30),
sslc varchar(30),
hsc varchar(30),
educationtype varchar(30),
cname varchar(30),
qualification varchar(30),
tarea varchar(30)
)
```
